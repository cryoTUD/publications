name: faraday2022
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pip
  - pip:
    - locscale
    - typing_extensions
  # ── Jupyter / notebook execution ──────────────────────────────────────────
  - jupyter
  - nbconvert
  - ipykernel


  # ── Excel manifest output ─────────────────────────────────────────────────
  - openpyxl
